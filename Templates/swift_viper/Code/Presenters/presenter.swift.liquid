{% include 'header' %}
import UIKit

class {{ module_info.name }}Presenter {
    private let interactor: {{ module_info.name }}Interactor
    private let wireframe: {{ module_info.name }}Wireframe
    
    init(interactor: {{ module_info.name }}Interactor, wireframe: {{ module_info.name }}Wireframe) {
        self.interactor = interactor
        self.wireframe = wireframe
    }
}
